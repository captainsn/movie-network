extends template 

//-block content
	#container 
		table#board 
			- for (i = 0; i < 3; i++)
				tr 
					- for (x = 0; x < 3; x++)
						- var index = (i * 3) + x 
						td-//

block content 
	- var ratingAry = [{"rating":"1"},{"rating":"2"},{"rating":"3"},{"rating":"4"},{"rating":"5"},{"rating":"6"},{"rating":"7"},{"rating":"8"},{"rating":"9"},{"rating":"10"},{"rating":"0"},{"rating":"11"}]
	- var newAry = ratingAry.slice(0);
	- ratingAry.sort(function(a, b){
		- return b.rating-a.rating
	- })
	- for (var i = 0; i < ratingAry.length; i++) {
		- ratingAry[i].order = i
	- }
	.button-wrapper
		button#submit(type='button' onclick='createPlaylist()') Create Playlist
	.button-wrapper
		button#default Relevance
		button#high-to-low Highest to Lowest
		button#low-to-high Lowest to Highest
		button#default Your Friends
	ul#grid
		each val, index in newAry
			li(class="sortable", data-order=val.order,data-default=index)
				i.fa.fa-picture-o.fake-image
				span.rating= "Playlist Name " + val.rating
	